# Redis production config for the private Compose network.
# No host ports are published. AUTH (requirepass) is not enabled yet because
# the Node client only reads REDIS_HOST / REDIS_PORT.
bind 0.0.0.0
port 6379
protected-mode no
dir /data
appendonly yes
appendfsync everysec
save 60 1000
