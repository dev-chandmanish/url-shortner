# Redis production config for the private Compose network.
# No host ports are published. Password auth is supplied at runtime via
# redis-server --requirepass from REDIS_PASSWORD (not stored in this file).
bind 0.0.0.0
port 6379
protected-mode no
dir /data
appendonly yes
appendfsync everysec
save 60 1000
